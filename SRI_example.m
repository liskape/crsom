function [ result ] = SRI_example( crsom, in, ta, i)
N = length(in);
M = length(unique(vec2ind(ta)));
NEURONS = length(crsom.IW{1});

win = vec2ind(ta(:,i));
C = vec2ind(ta(:,i));
H = 

inner = zeros(M);

for j = 1:M
 
    inner(j) 
end


end

